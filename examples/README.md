# Examples

- `nixos/`: minimal `services.uriel-worker` module snippet.
- `home-manager/`: minimal `programs.uriel` module snippet.
- `hetzner-tailscale/`: complete Hetzner + Tailscale host flake, deployable
  with nixos-anywhere.
